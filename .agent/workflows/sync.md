---
description: Sync with the official Stanford assignments repository.
---

// turbo-all

Steps:
1. Ensure the `references/office-code/` directory exists.
2. Clone or fetch the latest from `https://github.com/mihail911/modern-software-dev-assignments`.
3. Sync themes and content into the local reference folder.
4. Log the sync status in `docs/WORKLOG/`.
