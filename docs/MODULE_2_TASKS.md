# Module 2 Task Tracker (Python Essentials)

目标：在 AI IDE 辅助下系统学习 Python 核心语法体系。

## TODO List
- [ ] **环境核验实验 (ImportError)**：设计一个对比实验。
    - 操作：让学员在未安装特定库的环境（如系统环境）运行代码产生 `ImportError`，再切换到已配置好的 `ai_course` 环境运行。
    - 目的：建立“解释器环境与运行代码强绑定”的直觉认知。
- [ ] **进阶插件引入**：在涉及复杂缩进或调试时，适时引入 `Indent-Rainbow` 和 `Error Lens` 扩展，提升开发体验。
