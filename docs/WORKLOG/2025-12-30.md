# 2025-12-30

## 今日目标
- 同步官方 Stanford 课程仓库到本地 `references/office-code/`。

## 完成事项
- 已克隆官方仓库到 `references/office-code/`。
- 当前版本：`ca2df55 edit assignment.md`。

## 复盘
- 首次同步成功，无本地改动冲突。

## 结论与改进
- 后续同步前先检查 `references/office-code/` 是否有本地改动。

## 明日计划
- 视需要对比官方内容与本地课程结构。

## 同步补记
- 将 `references/office-code/week1` 的同名差异文件覆盖到 `curriculum/Stanford CS146S/week1`。
- 覆盖文件：`assignment.md`、`chain_of_thought.py`、`k_shot_prompting.py`、`rag.py`、`reflexion.py`、`self_consistency_prompting.py`、`tool_calling.py`。

## 结构调整补记
- `curriculum/Stanford CS146S/week1` 仅保留 `.md` 课件。
- 已将两份官方 PDF 课件移入 `references/office-code/week1/`。
- 清理了 `curriculum/Stanford CS146S/week1` 内的非 md 文件与空目录。

## 清理补记
- 移除根目录遗留文件：`.DS_Store`、`.gitignore`、`README_EXPLAINED.md`、`poetry.lock`、`pyproject.toml`。
- 重写根目录 `README.md` 为项目心得说明。

## 结构与文档优化补记
- 新增顶层分组：`curriculum/`（课程内容）与 `references/`（官方快照与参考课程）。
- 已移动目录：`Learn Python with AI IDE`、`Stanford CS146S`、`office-code`、`ng_lesson` 到新结构。
- 更新所有路径引用：`README.md`、`docs/ACTION_GUIDE.md`、`docs/MAPPING.md`、`docs/WEEK1_TASKS.md`、`skills/office-code-sync/SKILL.md` 等。
- 精简各级 README 为索引式结构，便于快速上手。

## 子模块与入口补记
- 将 `references/office-code/` 设为 git submodule，并在文档中标注初始化步骤。
- 更新 `README.md` 说明 submodule 初始化命令。
- 更新 `skills/office-code-sync/SKILL.md` 的同步流程。

## 解耦补记
- 移除 `references/office-code/` 子模块配置与 `.git` 元数据，改为完全本地化目录。
- 更新 `README.md` 与 `docs/ACTION_GUIDE.md` 去除 submodule 说明。
- 更新 `skills/office-code-sync/SKILL.md`，改为临时目录拉取并覆盖本地快照的同步流程。
