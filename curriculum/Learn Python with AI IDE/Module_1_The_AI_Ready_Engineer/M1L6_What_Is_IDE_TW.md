# Module 1 Lesson 6: 效率倍增器：透過 IDE 進行工程開發

> **課程體系：Learn Python with AI IDE**
> **當前模組**：Module 1 - The AI Ready Engineer
> **本課目標**：入駐 AI IDE。理解「工具主權」，掌握界面佈局與環境配置，實現從「手動操作」到「工程自動化」的跨越。

---

### 🔙 前修回顧
在 **M1L5** 中，我們掌握了 Python 腳本的本質。我們透過最原始的記事本與終端機指令，手動完成了「編寫-儲存-執行」的閉環。這一過程雖繁瑣，但讓你看清了代碼運行的物理規則：腳本只是文本，而運行代碼本質上是調用解釋器去閱讀這份文本。

### 📅 今日預告
今天，我們將把之前五課學到的零散技能進行一次大整合。你會發現，之前在終端機（Terminal）裡練習的**路徑管理 (L2)**、**環境切換 (L4)** 以及**腳本執行 (L5)**，都將集成在一個統一的界面中。

我們將學習如何配置 **Antigravity** 這款 AI IDE。這個「今日預告」並不是要讓你去拿回所謂的工具主權，而是要讓你從一開始就建立起**工具主權**的概念。不要讓 AI 成為你的拐杖，而要讓它成為你手中的利劍。

---

## 1. Philosophy: 工具主權與 AI IDE 生態

### 1.1 主流 IDE 與 AI 生態
在正式開始前，我們需要再次明確 IDE 的定義，並了解目前工程界主流的開發工具：

*   **IDE (Integrated Development Environment)**：整合開發環境。它不僅僅是一個寫代碼的文本編輯器，而是一個集成了檔案管理、代碼編輯、終端機控制和調試功能的「軍火庫」。
*   **AI IDE**：在傳統 IDE 的基礎上，深度集成了 AI 能力的開發工具。
    *   **Antigravity**：我們課程使用的教學版 IDE。
    *   **Cursor**：業界首款流行的 AI 原生 IDE。
    *   **Windsurf**：由 Codeium 出品，其 Flow 功能提供了規範的 AI 協作編程方式。
    *   **Kilo**：新興的 AI 原生 IDE，專注於極致性能與 AI 深度融合。
    *   **Trae**：字節跳動出品的 AI IDE。
*   **AI 增強外掛**：即使是傳統的 IDE (如 VS Code)，也可以透過安裝外掛獲得 AI 輔助能力。這些外掛包括：
    *   **Gemini CLI Companion**
    *   **Gemini Code Assist**
    *   **Claude Code for VS Code**
    *   **Codex – OpenAI’s coding agent**


---

## 2. The Raw Logic：界面感知與「生存配置」

### 2.1 界面初始化 (The Initial Setup)
打開 Antigravity 後，我們需要將界面調整為最經典的「工程模式」。請熟悉以下三個核心區域及其快捷鍵區分：

1.  **檔案總管 (Explorer) - 左側欄**
    *   **功能**：管理工程資產，瀏覽檔案結構。
    *   **快捷鍵**：
        *   **Mac**: `Cmd + Shift + E`
        *   **Windows**: `Ctrl + Shift + E`

2.  **編輯器 (Editor) - 右上核心區**
    *   **功能**：編寫代碼的主戰場。
    *   **快捷鍵**：`Cmd/Ctrl + 1` (快速切回第一個編輯視窗)

3.  **終端機面板 (Panel/Terminal) - 底部控制區**
    *   **功能**：執行指令，查看輸出。
    *   **快捷鍵**：
        *   **Mac**: `Cmd + J` 或 `Ctrl + ~`
        *   **Windows**: `Ctrl + J` 或 `Ctrl + ~`

此外，左側通常還有一個 **AI 側邊欄 (Sidebar Agent)**，暫時將其**隱藏**，我們需要專注於核心工程界面。

現在，你應該看到一個穩定的佈局：**左側檔案總管**、**右上編輯區**、**下部終端機區**。

### 2.2 核心圖標與延伸模組 (Extensions)
在左側邊欄，你會看到幾個核心功能圖標。作為 Python 工程師，最常用的是：
*   **檔案總管 (Explorer)**：管理當前項目的資料夾和檔案。
*   **延伸模組 (Extensions)**：安裝增強功能的「外掛商店」。

### 2.3 宿主與延伸模組的關係
IDE 就像一個精密的硬體支架（宿主），而**延伸模組 (Extensions)** 則是為它注入靈魂的功能模組。
1.  **Python 延伸模組**：這是必備組件。它賦予了 IDE 識別、執行、調試和智能提示 Python 代碼的能力。
2.  **中文語言包 (Traditional)**：安裝後重新啟動，界面將切換為繁體中文。

---

## 3. Instruction：關聯環境與項目

### 3.1 搬入項目資料夾
將我們在 M1L5 中辛苦打磨的「第一批移民」搬進 IDE。這也是我們在 L5 手動創建的那三個 `.py` 腳本檔案（`hello.py` 等）。
*   點擊菜單欄 `文件 (File)` -> `開啟資料夾 (Open Folder)`。
*   選擇該資料夾。此時，你可以在左側看到所有的檔案。

### 3.2 配置解釋器：插座與插頭 (Recall M1L4)
> **🔌 隱喻回調**
> 環境 `ai_course` 是我們的**專用電源插座**。現在，我們要為 IDE 這台設備接通電源。

我們有兩種方式來完成這次「通電」：

**方式 A：透過指令面板 (標準做法)**
1.  呼出**指令面板** (`Cmd/Ctrl + Shift + P`)。
2.  搜尋並選擇 `Python: Select Interpreter` (選擇解釋器)。
3.  在列表中選中帶有 `ai_course` 標識的那一項。

**方式 B：透過狀態欄 (快捷做法)**
1.  打開任意一個 `.py` 檔案（例如 `hello.py`）。
2.  看向 IDE 的**右下角狀態欄**，你會看到顯示當前 Python 版本的地方（或者顯示 `Select Interpreter`）。
3.  直接點擊它，這會彈出和方式 A 一樣的選擇列表。
4.  選擇 `ai_course`。

*操作完成後，請確認右下角已顯示 `ai_course` 字樣。*

---

## 4. Practice：複刻與驗證

### 4.1 創建 IDE 演示腳本
1.  **創建檔案**：在左側**檔案總管**中右鍵點擊資料夾空白處，選擇 `新建檔案 (New File)`，命名為 `ide_demo.py`。
2.  **進入編輯狀態**：單擊剛創建的 `ide_demo.py`，你會在右上的**編輯區**看到一個空白的輸入視窗。
3.  **編寫代碼**：就像在記事本中輸入文字一樣，在視窗中輸入：
    ```python
    print("Hello AI IDE")
    ```
    *你會發現：輸入 `pri` 時會出現提示補全。這其實是 IDE 安裝了 Python 模組後的「詞典映射」功能，它能認出語法。未來我們加裝 AI 引擎後，它將不再只是認詞，而是能夠理解你的意圖。*

### 4.2 運行對比：手動 vs 一鍵
*   **方法 A (傳統終端機)**：在 IDE 底部的**終端機**裡，手動輸入 `python ide_demo.py`。
*   **方法 B (IDE 一鍵執行)**：點擊編輯器右上角的**執行按鈕**（三角形圖標）。

**硬核觀察**：點擊執行按鈕後，請死死盯著底部終端機。你會發現 IDE 幫你輸入了一行極長的指令。
**結論**：IDE 並不是魔法，它是一個**集成的自動化平臺**。除了運行代碼，未來你還將在這裡管理資料庫、管理 Git 倉庫。它是你作為工程師的**唯一完整工作站**。

---

## 5. Engineering Checkpoint (工程自檢)

### ✅ 驗收標準 (Pass Criteria)
1.  **界面對齊**：左側、右上、下部三個區域清晰分佈，AI 側邊欄已收起。
2.  **模組就緒**：安裝了 Python 模組，界面顯示為繁體中文。
3.  **環境通電**：執行 `ide_demo.py` 無誤。此時，IDE 的右下角狀態欄應明確顯示當前使用的是 `ai_course` 環境（而不是系統的 Base 環境）。

---

## 6. Philosophy: 工具主權 (Tool Sovereignty)

在課程的最後，我們回到最開始的問題：為什麼在 AI 能幫我們寫代碼的時代，我們還要學習 IDE 的配置和原理？

*   **從「代碼主權」到「工具主權」**：在 Module 1 的前幾課，我們透過手寫代碼、手動運行，掌握了對**代碼**的掌控權。而現在，透過掌握 IDE，我們拿回了對**生產工具**的掌控權。
*   **新的檢驗標準**：在未來的學習和工作中，AI 確實可以幫我們代勞大部分編碼工作。但是，**你需要知道它做了什麼**。
    *   以前，你檢驗的是**自己**寫的一行代碼對不對。
    *   以後，你檢驗的是**AI** 幫你生成的一整個功能、一次環境配置、一次重構對不對。

**工具主權**的本質，就是具備**「審查 AI」**的能力。只有當你深刻理解這個協作平台是如何運作的，你才不會被 AI 的幻覺帶偏，才能真正成為人機協作中的 Command (指揮官)。

---

**在未來的 Module 2 中，我們將以 IDE 為核心平台，系統性地學習 Python 的所有核心知識。屆時，每當你學到一個知識點，我們都會立刻在 IDE 中運行並驗證它。**

**在下一課中，我們將探討「數位藍圖」：學習如何科學地組織你的項目結構，讓代碼更具可移植性。**
