# Module 1 Lesson 3: 即時通訊：Python REPL (System)

> **課程體系：Learn Python with AI IDE**
> **當前模組**：Module 1 - The AI Ready Engineer
> **本課目標**：喚醒系統原生 Python 引擎，深入理解「解譯型語言」的互動本質，掌握專業級的環境內省 (Introspection) 指令。

---

在上一課 [M1L2] 中，我們揭開了終端機（Terminal）與 Shell 協作的秘密，理解了指令是如何透過映射表（$PATH）被系統搜尋並執行的。現在，你已經掌握了在這片數位密林中移動與探索的「羅盤」。由於 Python 是一種解譯型語言，我們不需要像傳統軟體工程那樣經歷漫長的編譯等待，而是可以直接喚醒系統原生的引擎進行「即時對話」。掌握 REPL（互動式解譯器）是為了讓我們在編寫複雜的自動化腳本之前，獲得一個可以隨時驗證邏輯、探查物件本質的實驗室。這是從靜態閱讀程式碼轉向動態掌控邏輯的關鍵跳躍。

## 1. 環境自檢：你的電腦裡有 Python 嗎？

在喚醒這台引擎之前，我們必須確認它是否已經安裝在你的電腦裡。

請打開上一課掌握的 **Terminal**，輸入以下命令並按下 Enter：

```bash
# Mac 用戶輸入這個：
python3 --version

# Windows 用戶輸入這個：
python --version
```

### ✅ 成功的情況
如果你看到類似 `Python 3.x.x` 的回覆（如 `Python 3.10.2`），恭喜！你的系統裡已經預裝了引擎，可以直接跳到 **第 2 步**。

### ❌ 失敗的情況
如果終端機回覆 `command not found` 或沒有任何反應：
1.  這說明你的電腦還是「出廠設定」。
2.  請前往 Python 官網下載安裝包：[python.org](https://www.python.org/downloads/)
3.  下載最新版（黃色按鈕），一路點擊「下一步」安裝即可。
    *   **重要提示 (Windows)**：安裝時務必勾選 **"Add Python to PATH"**（將 Python 添加到環境變數），否則你在終端機裡還是找不到它！

---

## 2. 什麼是 REPL (Read-Eval-Print Loop)？

既然引擎已經就位，我們不需要像寫作文一樣寫完整個檔案才執行。Python 允許我們直接「對話」。

**REPL** 是 Python 的「即時聊天視窗」，它的原理是四個步法循環往復：
1.  **Read (讀)**：讀取你輸入的程式碼。
2.  **Eval (算)**：執行邏輯。
3.  **Print (印)**：把結果列印在螢幕上。
4.  **Loop (回)**：等待下一條指令。

**工程直覺**：REPL 是為了**快速實驗**。當你不知道某行程式碼會不會報錯時，先在這裡試一下，是高級工程師的日常習慣。

---

## 3. 建立連接：呼叫解譯器

繼續在終端機裡，輸入對應的啟動暗號：

*   **Mac**: 輸入 `python3` 並按下 Enter。
*   **Win**: 輸入 `python` 並按下 Enter。

當你看到 **`>>>`** 這個符號出現時，說明你已經成功「黑入」了 Python 的內部。

**⚠️ 警告**：此時你已經離開了 Shell。剛才學的 `ls`、`cd` 在這裡通通失效。你現在是在和 Python 對話，不是作業系統。

---

## 4. 專業級互動實驗

現在讓我們在這個「沙盒」裡做點實驗。

### 4.1 變數與記憶體（Namespace）
逐行輸入以下程式碼：
```python
>>> x = 3.14
>>> y = 2
>>> x * y
6.28
```
**底層邏輯**：這些變數現在活在記憶體裡。只要你不關掉視窗，它們就一直存在。

### 4.2 內省 (Introspection)：工程師的顯微鏡
與其瞎猜，不如直接問 Python。這是 AI 時代防幻覺的最強工具：

*   **`type(x)`**：問 Python "x 是什麼型態？"（它會告訴你是 float 浮點數）。
*   **`help(print)`**：查看 `print` 函式的官方說明書（按 `q` 退出說明書）。
*   **`globals()`**：看看現在的記憶體裡到底存了哪些全域變數。

---

## 5. 機制對齊：REPL vs. 腳本 (Script)

這是新人最常有的困惑：「為什麼我在 REPL 裡輸入 `1 + 1` 會出 `2`，但在資料夾裡的 `.py` 檔案寫 `1 + 1` 執行後什麼都不顯示？」

*   **REPL 邏輯**：為了方便，它自帶「自動列印」。
*   **Script 邏輯**：為了嚴謹，如果你不寫 `print()`，程式就會默默計算完然後結束，不會告訴你任何事。

**結論**：在寫檔案時，永遠不要忘了 `print()`。

---

## 6. 異常追蹤 (Traceback)

當程式碼崩壞時，REPL 會吐出一段報錯。別怕，這是它在告訴你錯哪了。
```python
>>> 10 / 0
ZeroDivisionError: division by zero
```
這就是著名的「除以零錯誤」。讀懂報錯是工程師的第一課。

---

## 7. 緊急脫離：逃離「黑洞」

很多新手在 `>>>` 介面想回 Shell 做 `ls` 操作，結果因為沒退出而在 Python 裡瘋狂報錯。

*   **快速鍵退出 (推薦)**：
    *   Mac: `Ctrl + D`
    *   Win: `Ctrl + Z` 然後按下 Enter。
*   **顯式退出**：輸入 `exit()` 並按下 Enter。

---

## 8. 工程自檢 (Engineering Checkpoint)

### ✅ 驗收標準 (Pass Criteria)
1.  **環境確認**：在終端機執行 `python --version` (或 `python3`) 能看到版本號。
2.  **拉起**：成功透過終端機進入 `>>>` 介面。
3.  **偵測**：
    *   定義 `a = "AI"`。
    *   執行 `type(a)` 確認它是字串 (str)。
    *   執行 `a.upper()` 看到 'AI'。
4.  **內省**：執行 `globals()`，確認 `a` 確實在記憶體列表中。
5.  **脫出**：透過快速鍵快速切回 Shell。

### 🛑 常見故障 (Fail Patterns)
*   **誤區**：在 `>>>` 下敲 `ls` —— 只有退出到 Shell 才能控操作業系統。
*   **誤區**：Windows 安裝時沒勾選 "Add to PATH"，導致輸入 `python` 說找不到命令。

---

> [!IMPORTANT]
> **本課結語**
>
> 我們剛剛完成了與系統原生 Python 的「第一次握手」。
>
> 然而警告：直接給系統自帶的 Python 安裝套件是一個極大的工程禁忌。
> 那樣會把原本乾淨的系統搞得一團糟。我們要養成「實驗室思維」——為每個專案搭建一套隔離的虛擬環境。
>
> **下一課：Conda —— 打造你的工程無塵實驗室。**
