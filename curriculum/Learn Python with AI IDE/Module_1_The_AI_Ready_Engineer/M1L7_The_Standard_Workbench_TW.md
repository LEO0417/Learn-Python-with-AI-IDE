# Module 1 Lesson 7: 工程底座：構建你的「標準工作台」

> **課程體系：Learn Python with AI IDE**
> **當前模組**：Module 1 - The AI Ready Engineer
> **本課目標**：掌握檔案總管的操作邏輯，學會透過「工作台」思維科學組織工程資產。理解相對路徑的底層含義，並掌握多終端機管理技巧，構建高效的並行開發流。

---

### 🔙 前修回顧
在 **M1L6** 中，我們已經完成了對 **Antigravity** 這座「全能工作站」的初步熟悉。我們了解了視窗分區和基本設置，但這還只是「毛坯房」狀態。

### 📅 今日預告：構建協作的「共同邊界」
我知道你迫切想要激活 AI 助理開始編寫代碼，但在這之前，我們必須先建立**標準工作台 (The Workbench)**。

這一步的必要性在於：
1.  **劃定協作邊界**：現代 AI IDE 的核心能力基於「索引」。建立標準目錄（如 `scripts` 和 `data`）能顯著減少 AI 的歧義與幻覺，確保它在受控的環境中協助你。
2.  **構建並行視野**：除了管理檔案，我們還將學習**多終端機佈局**。掌握如何分屏、分分頁管理執行區與核驗區，是你從「單任務新手」向「多任務工程師」轉型的必備素養。

打好這個底座，我們才能在下一課中，讓 AI 助理在精準的環境中為你效力。

---

## 1. 工程邏輯：工作台的秩序與效率

### 1.1 什麼是專案？(The Project)
在程式設計中，**專案 (Project)** 包含了一次開發任務所需的所有代碼、數據和設定檔案。
*   **初級視角**：專案等同於一個普通的資料夾。
*   **專業視角**：專案是你的「工程基座」。你需要清晰地劃定：代碼邏輯（scripts）、原材料（data）和說明文件（README）的存儲邊界。

### 1.2 為什麼必須規範化？
1.  **可移植性 (Portability)**：規範的結構能確保你的代碼在分享給他人或換台電腦時，依然能直接執行。
2.  **更有效地與 AI 協作**：理解根目錄、子目錄、相對路徑，能讓你更清晰地向 AI 表達你的需求，讓協作更精準。

---

## 2. 理解映射：IDE 檔案總管與物理磁碟

IDE 左側的**檔案總管 (Explorer)** 並不是一個獨立的虛擬空間，它是**本地磁碟資料夾在 IDE 中的「即時鏡像」**。

### 2.1 即時同步邏輯
你可以進行以下驗證：
1.  在 **Explorer (檔案總管)** 側邊欄的檔案列表下方空白處點擊右鍵，選擇「新建資料夾」，命名為 `my_demo`。
2.  打開你的物理磁碟窗口，你會發現資料夾瞬間同步生成了。

**💡 效率貼士**：忘記側邊欄在哪了？試試 `Cmd + B` (Mac) 或 `Ctrl + B` (Windows) 來快速切換顯示它。

---

## 3. 核心概念：絕對路徑 vs 相對路徑

這是區分「業餘玩家」與「專業工程師」的關鍵認知。

### 3.1 座標 vs 門牌號
*   **絕對路徑 (Absolute Path)**：就像地球上的「經緯度」。
    *   例如：`C:\Users\Leo\Desktop\project\data.csv`。
    *   **局限性**：如果你把專案發給朋友，他電腦上可能沒有相同的資料夾結構（比如用戶名不同），代碼就會報錯。
*   **相對路徑 (Relative Path)**：就像是「在這個大樓的二樓左側」。
    *   例如：`./data.csv`。
    *   **優勢**：無論這棟大樓整體搬遷到哪裡（拷貝到哪個槽或哪台電腦），只要你在專案裡，總能透過相對位置找到檔案。

**建議**：**盡量在代碼中使用相對路徑**。這樣編寫的代碼更靈活，也更容易分享。

---

## 4. 重點實驗：重構並驗證「工作台」

### 4.1 搬遷資產
在 IDE 的檔案總管中：
1.  **創建目錄**：新建資料夾 `scripts` 和 `data`。
2.  **移動資產**：將 `ide_demo.py` 以及之前的腳本全部拖入 `scripts` 中。

### 4.2 代碼驗證：我是誰？他在哪？
1.  在 `scripts/` 下新建一個檔案 `path_check.py`，寫入：
    ```python
    import os
    # 打印當前的工作目錄
    print("當前工作台錨點 (WorkDir):", os.getcwd())
    ```

> **💡 代碼小科普：**
> - **`import os`**：就像是叫來一名「專業向導」（Operating System），它專門負責指揮 Python 與作業系統打交道。
> - **`os.getcwd()`**：翻譯過來就是：「向導，請告訴我，我現在所在的這個工作台錨點在哪？」
> 
> *註：關於它們的底層語法細節，我們在 **Module 2** 深入攻克 Python 核心概念時會詳細拆解，現在你學要把它當成一個「位置探測器」即可。*

2.  **執行並觀察**：你會發現，IDE 預設將你**開啟資料夾**的那一層級識別為整個工程的錨點。

---

## 5. 進階：管理你的動態儀表盤 (Terminal Tabs)

一個高效的工程師不會只有一個儀表盤。在真正的開發場景中，我們需要**「並行操作」**。

### 5.1 實戰案例：靈活的「雙儀表盤」模式

專業的工程師絕不會被單一的視窗限制死。雖然終端機的使用沒有標準答案，但為了提高效率，我們可以嘗試一種通用的**「執行與核驗分離」**佈局作為參考：

1.  **場景模擬 A (執行區)**：在一個視窗專門負責執行代碼（例如 `python scripts/path_check.py`）。這個視窗主要用來觀察代碼吐出來的即時結果。
2.  **場景模擬 B (核驗區)**：利用 **Split (分屏)** 或新建分頁開啟第二個視窗。當代碼正在執行時，你可以在這裡從容地輸入 `conda info` 檢查環境，或者用 `ls -R` 確認檔案搬遷是否成功，而不需要中斷第一個視窗的輸出。

**💡 儀表盤哲學 (Dashboard Philosophy)：**
這就像駕駛賽車：**「擋風玻璃」**看路況，**「儀表盤」**看數據。這種分工讓你能實現視線在「執行結果」與「系統狀態」之間的秒級切換，保持「感知不中斷」。

**🔥 高級進階技巧：將終端機「推向重心」**
如果你正在處理一個非常複雜的邏輯，終端機的輸出可能比代碼本身更重要。這時，你可以**用滑鼠按住終端機的標籤欄，將其直接拖拽到螢幕右上方的編輯器區域**。
*   這樣，終端機就會像一個代碼檔案一樣佔據視覺中心，讓你擁有更大的視野來監控那些滾滾而來的執行數據。
*   **記住**：佈局是活的，根據你當前的「戰鬥重點」隨時調整視角，才是真正的工程化思維。

---

## 6. 自測與驗收 (Checkpoint)

### ✅ 驗收標準
1.  **目錄就緒**：根目錄下已建立規範的 `scripts` 等資料夾，僅保留 `README.md`。
2.  **路徑理解**：了解什麼是相對路徑及其優勢。
3.  **操作自如**：能熟練開啟多終端機分頁，並能嘗試拖拽調整佈局。

---

## 7. 小結複盤：今日知識點回顧

在結束今天的課程之前，讓我們快速回顧一下你剛剛掌握的「標準工作台」技能：

1.  **物理與邏輯的映射**：檔案總管即時反映了你硬碟上的檔案變動。
2.  **路徑的應用**：**相對路徑**透過「工作目錄」作為參考，讓專案具備了跨環境執行的靈活性。
3.  **多任務處理**：學會透過分屏和多分頁管理終端機，實現「感知不中斷」的並行開發。

**更深層級意義的昇華**：
通過這一課，你建立的不只是資料夾，而是一種**工程認同感**。你會逐漸意識到，不僅是代碼，那些讓 AI 變聰明的「規範文檔」和「核心技能（Skills）」，本質上都是存放於特定目錄下的實體檔案。掌握了目錄佈局，你就掌握了調動 AI 高階能力的物理鑰匙。

---

## 8. 下一站：激活你的 AI 助理側邊欄

秩序本身就是生產力。今天你打下的所有基礎，實際上都是為了迎接那個真正的「重頭戏」。

在下一節課中，我們將正式推開 AI IDE 的大門——**激活 AI 助理側邊欄**。

我們將一起探索 AI 如何透過你親手建立的這套工作台，實現跨檔案、跨場景的深度協作，帶你完整感受 AI IDE 的協作效能。
