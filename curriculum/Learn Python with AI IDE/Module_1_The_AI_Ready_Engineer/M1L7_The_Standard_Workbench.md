# Module 1 Lesson 7: 工程底座：构建你的「标准工作台」

> **课程体系：Learn Python with AI IDE**
> **当前模块**：Module 1 - The AI Ready Engineer
> **本课目标**：掌握资源管理器的操作逻辑，学会通过“工作台”思维科学组织工程资产。理解相对路径的底层含义，并掌握多终端管理技巧，构建高效的并行开发流。

---

### 🔙 前修回顾
在 **M1L6** 中，我们熟悉了 **Antigravity** 这座“全能工作站”的基本布局，并配置了 Python 插件与解释器。如果说 L6 是带你“参观了车间并接通了电源”，那么今天，我们要正式“规划仓库”并“熟悉控制台”。

### 📅 今日预告：两大核心支柱
我知道你迫切想要激活 AI 助理，但在邀请它进入项目之前，我们必须先打磨好工作台的两个核心支柱：**资源管理器 (Explorer)** 与 **终端 (Terminal)**。

1.  **资源管理器 (静态管理)**：不仅仅是存代码的地方，更是你与 AI 的“共享记忆库”。
2.  **终端 (动态管理)**：这里是 AI 的“执行演武场”。未来 AI 会在这里为你安装库、运行测试、甚至开启多个并行任务。掌握管理终端的显示习惯，是为了让你在 AI “左右开弓”时，能清晰地俯视它的每一个动作。

打好这个底座，是为了让 AI 助理在下一课进场时，能在一个有序的环境中精准为你效力。这正是 **AI Ready Engineer** 的核心逻辑：不仅能写代码，更能设计让 AI 稳定运行的“工程环境”。

---

## 1. 工程逻辑：工作台的秩序与效率

### 1.1 什么是项目？(The Project)
在编程中，**项目 (Project)** 包含了一次开发任务所需的所有代码、数据和配置文件。
*   **初级视角**：项目等同于一个普通的文件夹。
*   **专业视角**：项目是你的“工程基座”。你需要清晰地划定：
    *   **逻辑 (Scripts)**：你写的代码文件。
    *   **原料 (Data)**：项目运行需要的数据。
    *   **说明书 (README)**：项目的“身份证”，告诉别人（以及 AI）这个项目是干什么的。

### 1.2 为什么必须规范化？
1.  **可移植性 (Portability)**：规范的结构能确保你的代码在分享给他人或换台电脑时，依然能直接运行。
2.  **更高效地与 AI 协作**：理解根目录、子目录、相对路径，能让你更清晰地向 AI 表达你的需求，让协作更精准。

---

## 2. 理解映射：IDE 资源管理器与物理磁盘

IDE 左侧的**资源管理器 (Explorer)** 并不是一个独立的虚拟空间，它是**本地磁盘文件夹在 IDE 中的“实时镜像”**。

### 2.1 为什么它如此重要？
资源管理器是你工程资产的“物理真相”。
*   **对于你**：它是代码、数据、文档的栖息地。
*   **对于 AI**：这是关键点——**AI 的“大脑”也是文件**。在 Antigravity 中，AI 的长期记忆（Skills）和工作流（Workflows）本质上都是存储在 `.agent` 等特定文件夹下的 `.md` 文档。
*   **结论**：掌握了资源管理器，你就掌握了查看和修改 AI 认知的权限。

### 2.2 实时同步逻辑
你可以进行以下验证：
1.  在 **Explorer (资源管理器)** 侧边栏的文件列表下方空白处点击右键，选择“新建文件夹”，命名为 `my_demo`。
2.  打开你的物理磁盘窗口，你会发现文件夹瞬间同步生成了。

**💡 效率贴士**：忘记侧边栏在哪了？试试 `Cmd + B` (Mac) 或 `Ctrl + B` (Windows) 来快速切换显示它。

---

## 3. 核心概念：绝对路径 vs 相对路径

这是区分“业余玩家”与“专业工程师”的关键认知。

### 3.1 坐标 vs 门牌号
*   **绝对路径 (Absolute Path)**：就像地球上的“经纬度”。
    *   例如：`C:\Users\Leo\Desktop\project\data.csv`。
    *   **局限性**：如果你把项目发给朋友，他电脑上可能没有相同的文件夹结构（比如用户名不同），代码就会报错。
*   **相对路径 (Relative Path)**：就像是“在这个大楼的二楼左侧”。
    *   例如：`./data.csv`。
    *   **优势**：无论这栋大楼整体搬迁到哪里（拷贝到哪个盘或哪台电脑），只要你在项目里，总能通过相对位置找到文件。

**建议**：**尽量在代码中使用相对路径**。这样编写的代码更灵活，也更容易分享。

---

## 4. 重点实验：重构并验证“工作台”

### 4.1 搬迁资产
在 IDE 的资源管理器中：
1.  **创建目录**：新建文件夹 `scripts` 和 `data`。
2.  **创建说明书**：在根目录下新建一个文件 `README.md`。暂时留空即可，它是我们下一课与 AI 协作的核心靶场。
3.  **移动资产**：将 `ide_demo.py` 以及之前的脚本全部拖入 `scripts` 中。

### 4.2 代码验证：我是谁？他在哪？
1.  在 `scripts/` 下新建一个文件 `path_check.py`，写入：
    ```python
    import os

    print("--- 路径分析仪表盘 ---")
    
    # 1. 我在哪？(The Anchor)
    # 打印当前的工作目录 (Current Working Directory)
    cwd = os.getcwd()
    print(f"当前工作台锚点 (CWD): {cwd}")

    # 2. 相对路径 vs 绝对路径演示
    # 假设我们要找 data 文件夹
    relative_path = "./data" 
    absolute_path = os.path.abspath(relative_path)

    print(f"相对路径 (我们写的): {relative_path}")
    print(f"绝对路径 (系统算的): {absolute_path}")
    ```

> **💡 代码小科普：**
> - **`import os`**：就像是叫来一名“专业向导”（Operating System），它专门负责指挥 Python 与操作系统打交道。
> - **`os.getcwd()`**：翻译过来就是：“向导，请告诉我，我现在所在的这个工作台锚点在哪？”
> 
> *注：关于它们的底层语法细节，我们在 **Module 2** 深入攻克 Python 核心概念时会详细拆解，现在你只需要把它当成一个“位置探测器”即可。*

2.  **运行并观察**：你会发现，IDE 默认将你**打开文件夹**的那一层级识别为整个工程的锚点。

---

## 5. 进阶：由静转动——管理你的动态仪表盘 (Terminal Tabs)

刚才我们处理的是“静态”的文件结构，但在实际开发中，AI 常常会化身为“自动驾驶员”，在终端里为你装环境、跑代码、改 Bug。如果你不熟悉多终端的操作，就很容易在 AI 同时开启多个任务流时感到“两眼一抹黑”。这就需要用到我们的第二大支柱——**终端 (Terminal)**。

一个高效的工程师不会只有一个仪表盘。在真正的开发场景中，我们需要**“并行操作”**。

专业的工程师绝不会被单一的窗口限制死。特别是在 AI 代劳的时代，你会经常遇到这种场景：AI 正在主终端运行一个耗时很久的任务，但你又想在旁边开个新窗口检查结果。

这就需要一种通用的**“运行与核验分离”**布局作为参考：

1.  **场景模拟 A (运行区)**：在一个窗口专门负责执行代码（例如 `python scripts/path_check.py`）。这个窗口主要用来观察代码吐出来的实时结果。
2.  **场景模拟 B (核验区)**：利用 **Split (分屏)** 或新建标签页开启第二个窗口。当代码正在运行时，你可以在这里从容地输入 `conda info` 检查环境，或者用 `ls -R` 确认文件搬迁是否成功，而不需要中断第一个窗口的输出。

**💡 仪表盘哲学 (Dashboard Philosophy)：**
这就像驾驶赛车：**“挡风玻璃”**看路况，**“仪表盘”**看数据。这种分工让你能实现视线在“运行结果”与“系统状态”之间的秒级切换，保持**“感知不中断”**。

**🔥 高级进阶技巧：将终端“推向重心”**
如果你正在处理一个非常复杂的逻辑，终端的输出可能比代码本身更重要。这时，你可以**用鼠标按住终端的标签栏，将其直接拖拽到屏幕右上方的编辑器区域**。
*   这样，终端就会像一个代码文件一样占据视觉中心，让你拥有更大的视野来监控那些滚滚而来的运行数据。
*   **记住**：布局是活的，根据你当前的“战斗重点”随时调整视角，才是真正的工程化思维。

---

## 6. 自测与验收 (Checkpoint)

### ✅ 验收标准
1.  **目录就绪**：根目录下已建立规范的 `scripts` 等文件夹，仅保留 `README.md`。
2.  **路径理解**：了解什么是相对路径及其优势。
3.  **操作自如**：能熟练开启多终端标签页，并能尝试拖拽调整布局。

---

## 7. 小结复盘：今日知识点回顾

在结束今天的课程之前，让我们快速回顾一下你刚刚掌握的「标准工作台」技能：

1.  **物理与逻辑的映射**：资源管理器实时反映了你硬盘上的文件变动。
2.  **路径的应用**：**相对路径**通过“工作目录”作为参考，让项目具备了跨环境运行的灵活性。
3.  **多任务处理**：学会通过分屏和多标签管理终端，实现“感知不中断”的并行开发。

**更深层级意义的升华**：
通过这一课，你建立的不只是文件夹，而是一种**工程认同感**。你正在从“开箱者”成长为真正的**总工程师**。你会逐渐意识到，不仅是业务代码，连那些让 AI 变聪明的“规范文档”和“核心技能（Skills）”，本质上都是存放于特定目录下的实体文件。掌握了目录布局，你就掌握了调动 AI 高阶能力的物理钥匙。

---

## 8. 下一站：激活你的 AI 助理侧边栏

秩序本身就是生产力。今天，你通过**资源管理器**利用好了“空间”，通过**终端**利用好了“时间”。

这为我们的下一位核心成员——AI 助理，铺平了道路。
*   因为有了清晰的目录结构（如 `.agent` 文件夹），AI 才能读取自己的技能和记忆。
*   因为有了标准的 `scripts` 和 `data` 分区，AI 才能精准地帮你写代码而不弄乱项目。

在下一节课中，我们将正式推开整合中心：掌握 AI IDE 的核心大门——**AI Sidebar Agent：上下文机制与执行授权管理** (L8)，看看它是如何在这个你亲手打造的「标准工作台」上大显身手的。
