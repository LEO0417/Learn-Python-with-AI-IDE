# Module 1 Lesson 4: 实时通讯：Python REPL

> **课程体系：Learn Python with AI IDE**
> **当前模块**：Module 1 - The AI Ready Engineer
> **前修回顾**：在 [M1L3] 中，我们掌握了在系统中穿梭的能力。
> **本课目标**：唤醒 Python 引擎，建立实时通讯链路 (REPL)，并掌握从“草稿”到“脚手架”的迁移工作流。

---

## 1. 什么是 REPL (Read-Eval-Print Loop)？

Python 是一种**解释型语言 (Interpreted)**。这意味着你可以像聊天一样，发给它一句代码，它立刻回你一句结果。
这种模式叫 **REPL**。

对于工程师来说，REPL 不是用来写软件的，它是用来**做实验**的。
*   *我不确定这个数学公式对不对？* -> REPL 里算一下。
*   *我不确定这个函数返回的是字符串还是数字？* -> REPL 里测一下。
*   *AI 给了我一段代码，我想验证第一行能不能跑通？* -> REPL 里试一下。

它是你的**快速原型 (Prototyping)** 实验室。

---

## 2. 建立连接：唤醒会话

### 2.1 启动
确保你的终端显示 `(ai_course)`，然后输入：
```bash
python
```
当提示符从 `$` (或 `>`) 变为 **`>>>`** 时，通讯建立。
你现在已经不在 Shell 里了，你在 Python 进程内部。

### 2.2 交互实验 (Interaction)

**实验 1：基础计算**
```python
>>> 1 + 1
2
>>> 20 * 5
100
```
*原理*：你不需要写 `print()`，REPL 默认会自动打印最后一行表达式的结果。

**实验 2：变量与其生命周期**
```python
>>> user = "Leo"
>>> level = 1
>>> level = level + 1
>>> level
2
```
*原理*：只要只要不关掉在这个窗口，`user` 和 `level` 这两个变量就会一直存活在内存中。

**实验 3：类型侦测 (Type Inspection)**
AI 经常搞混数据类型。作为工程师，你需要用 `type()` 来验明正身。
```python
>>> type(123)
<class 'int'>
>>> type("123")
<class 'str'>
```

---

## 3. 重要工作流：从 REPL 迁移到脚本

这是很多初学者缺失的一环。REPL 里的代码是**易失**的（关掉就没了）。
**工程习惯**：一旦在 REPL 里验证了一段逻辑是通的，必须立刻将其“固化”到文件中。

**实战动作**：
1.  在 REPL 里调试成功了一段逻辑（比如 `2 ** 10` 得到了 `1024`）。
2.  用鼠标选中那段有效的代码（不包含 `>>>`）。
3.  复制。
4.  新建一个文件（如 `scratch.py`），粘贴进去。
5.  **重要修正**：在脚本里，必须给它加上 `print()`，否则运行起来什么都看不到。
    *   REPL: `2 ** 10` (能看到结果)
    *   Script: `print(2 ** 10)` (必须显式打印)

---

## 4. 异常反馈：Traceback 是朋友

当你输入错误的指令：
```python
>>> 1 / 0
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
ZeroDivisionError: division by zero
```
不要害怕红色报错。
*   **Traceback**：案发现场。
*   **ZeroDivisionError**：案件原因。

学会阅读 Traceback，你就拥有了自我修复能力。

---

## 5. 退出：断开连接

当你需要回到 Shell 去操作文件系统（比如 `ls` 或 `git`）时，必须显式退出。

*   **方法 A (函数)**：输入 `exit()` 并回车。
*   **方法 B (信号)**：
    *   Mac: `Ctrl + D`
    *   Win: `Ctrl + Z` 然后回车。

如果不退出，在 `>>>` 后面输入 `ls`，Python 会报错，因为它不懂 Shell 的语言。

---

## 6. 工程自检 (Engineering Checkpoint)

### ✅ 验收标准 (Pass Criteria)
1.  **启动**：能顺利进入 `>>>` 模式，且不显示任何环境警告。
2.  **计算**：能完成一次简单的变量定义和计算（`x=1, y=2, x+y`）。
3.  **退出**：能熟练使用快捷键（Ctrl+D/Z）迅速切回 Shell 模式 (`$`)。

### 🛑 常见故障 (Fail & Triage)

*   **故障 1**：输入 `ls` 报错 `SyntaxError: invalid syntax`
    *   *诊断*：你忘记退出 Python 了。看左边是不是 `>>>`？如果是，请 `exit()`。
*   **故障 2**：`NameError: name 'x' is not defined`
    *   *诊断*：你试图使用一个还没定义的变量。或者你重启了 REPL，之前的内存已经被清空了。
*   **故障 3**：`python` 启动后版本显示 2.7 或 3.8（不是 3.12）
    *   *诊断*：你忘了 `conda activate ai_course`。请退出后激活环境。

### 🔄 记忆挑战
在 REPL 里定义 `x = 100`。
退出 REPL (`exit()`)。
再次进入 REPL (`python`)。
输入 `x`。
*   **预期**：报错。
*   **结论**：**REPL 的内存是暂时的。只有文件（Script）才是永恒的。**

---

> [!IMPORTANT]
> **本课结语**
>
> REPL 是你的草稿纸，让你能以最低成本测试猜想。
> 但要建造大厦，我们不能只靠草稿。
>
> **下一课，我们将学习如何将这些思维碎片组织成持久化的脚本文件。**
