# Module 1 Lesson 3: 实时通讯：Python REPL (System)

> **课程体系：Learn Python with AI IDE**
> **当前模块**：Module 1 - The AI Ready Engineer
> **前修回顾**：在 [M1L2] 中，我们掌握了终端。现在的你，就像一个站在机器面前的指挥官。
> **本课目标**：不安装任何额外东西，直接唤醒你电脑里沉睡的 Python 引擎，建立第一次通讯。

---

## 1. 什么是 REPL (Read-Eval-Print Loop)？

Python 是一种**解释型语言 (Interpreted)**。这意味着由于它的存在，计算机不再是冰冷的机器，而是一个可以对话的智能体。
你可以像聊天一样发给它一句代码，它立刻回你一句结果。
这种模式叫 **REPL**。

它不是用来写大型软件的，它是用来**做实验**的。
*   *我不确定这个数学公式对不对？*
*   *我不确定这个函数返回的是字符串还是数字？*
*   *我想临时当计算器用一下。*

---

## 2. 第一次接触 (First Contact)

### 2.1 唤醒
打开你的终端（Mac Terminal / Win PowerShell）。
这一步我们直接使用系统自带的 Python。

*   **Mac 用户**: 输入 `python3` 然后回车。（如果显示 command not found，试着输入 `python`）。
*   **Windows 用户**: 输入 `python` 然后回车。

当提示符从 `$` (或 `>`) 变为 **`>>>`** 时，通讯建立。
你现在已经不在 Shell 里了，你进入了 Python 的精神世界。

### 2.2 交互实验

**实验 1：向世界问好**
```python
>>> print("Hello World")
Hello World
```

**实验 2：数学计算**
```python
>>> 1 + 1
2
>>> 20 * 5
100
```
*原理*：在 REPL 里，你甚至不需要写 `print()`，它默认会像计算器一样显示结果。

**实验 3：变量与其生命周期**
```python
>>> user = "Leo"
>>> level = 1
>>> level = level + 1
>>> level
2
```
*原理*：只要你不关掉在这个窗口，`user` 和 `level` 这两个变量就会一直存活在内存中。

**实验 4：类型侦测 (Type Inspection)**
工程师只相信证据。
```python
>>> type(123)
<class 'int'>
>>> type("123")
<class 'str'>
```

---

## 3. 重要工作流：从 REPL 迁移到脚本

这是 REPL 最大的痛点：它是**易失**的。你关掉窗口，刚才写的所有代码瞬间化为乌有。

**工程习惯**：一旦在 REPL 里验证了一段逻辑是通的，必须立刻将其“固化”到文件中。

**实战动作**：
1.  在 REPL 里调试成功了一段逻辑（比如 `2 ** 10` 得到了 `1024`）。
2.  用鼠标选中那段有效的代码（不包含 `>>>`）。
3.  复制。
4.  新建一个文本文件（如 `scratch.py`），粘贴进去。
5.  **重要修正**：在脚本里，必须给它加上 `print()`，否则运行起来什么都看不到。
    *   REPL: `2 ** 10`
    *   Script: `print(2 ** 10)`

---

## 4. 异常反馈：Traceback 是朋友

当你输入错误的指令：
```python
>>> 1 / 0
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
ZeroDivisionError: division by zero
```
不要害怕红色报错。
*   **Traceback**：案发现场。
*   **ZeroDivisionError**：案件原因。

学会阅读 Traceback，你就拥有了自我修复能力。

---

## 5. 退出：断开连接

当你需要回到 Shell 去操作文件系统（比如 `ls` 或 `git`）时，必须显式退出。
很多新手在这个“黑洞”里出不来。

*   **官方方法**：输入 `exit()` 并回车。
*   **黑客快捷键**：
    *   Mac: `Ctrl + D`
    *   Win: `Ctrl + Z` 然后回车。

如果不退出，在 `>>>` 后面输入 `pwd`，Python 会报错，因为它听不懂 Shell 的语言。

---

## 6. 工程自检 (Engineering Checkpoint)

### ✅ 验收标准 (Pass Criteria)
1.  **启动**：在你的电脑上，能准确敲出 `python` 或 `python3` 进入 `>>>` 界面。
2.  **计算**：能完成一次简单的变量定义和计算（`x=1, y=2, x+y`）。
3.  **退出**：能熟练使用快捷键（Ctrl+D/Z）迅速切回 Shell 模式 (`$`)。

### 🛑 常见故障 (Fail & Triage)

*   **故障 1**：输入 `python` 说 `Command not found`。
    *   *Mac*：试着输入 `python3`。如果还不行，说明你系统真的很干净，那是好事！下一节课我们会安装。
    *   *Windows*：去应用商店搜 Python 安装一个（但更推荐等下一课装 Conda）。
*   **故障 2**：`SyntaxError` when typing `ls`
    *   *诊断*：你依然在 Python 里。请先 `exit()`。

### 🧠 思考题
可以发现，我们现在的 Python 是“裸奔”的。
*   它装在系统的哪个角落？
*   如果我想安装 Pandas 库，会装到哪里？会不会把系统搞乱？
*   如果我有两个项目，一个要 Python 3.8 一个要 3.12 怎么办？

---

> [!IMPORTANT]
> **本课结语**
>
> 我们刚刚成功“入侵”了系统的原生 Python。但这很不安全，也很不卫生。
> 直接使用系统 Python 进行开发，就像在自家在客厅里做化学实验——弄脏了没法收拾。
>
> **下一课，我们将建立一个真正的、隔离的“化学实验室” (Virtual Environment)。**
