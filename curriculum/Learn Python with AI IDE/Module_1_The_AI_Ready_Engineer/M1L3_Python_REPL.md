# Module 1 Lesson 3: 实时通讯：Python REPL (System)

> **课程体系：Learn Python with AI IDE**
> **当前模块**：Module 1 - The AI Ready Engineer
> **本课目标**：唤醒系统原生 Python 引擎，深入理解“解释型语言”的互动本质，掌握专业级的环境自省 (Introspection) 命令。

---

在上一课 [M1L2] 中，我们揭开了终端（Terminal）与 Shell 协作的秘密，理解了指令是如何通过映射表（$PATH）被系统搜寻并执行的。现在，你已经掌握了在这片数字密林中移动与探索的“罗盘”。由于 Python 是一种解释型语言，我们不需要像传统软件工程那样经历漫长的编译等待，而是可以直接唤醒系统原生的引擎进行“即时对话”。掌握 REPL（交互式解释器）是为了让我们在编写复杂的自动化脚本之前，获得一个可以随时验证逻辑、探查对象本质的实验室。这是从静态阅读代码转向动态掌控逻辑的关键跳跃。

## 1. 环境自检：你的电脑里有 Python 吗？

在唤醒这台引擎之前，我们必须确认它是否已经安装在你的电脑里。

请打开上一课掌握的 **Terminal**，输入以下命令并回车：

```bash
# Mac 用户输入这个：
python3 --version

# Windows 用户输入这个：
python --version
```

### ✅ 成功的情况
如果你看到类似 `Python 3.x.x` 的回复（如 `Python 3.10.2`），恭喜！你的系统里已经预装了引擎，可以直接跳到 **第 2 步**。

### ❌ 失败的情况
如果终端回复 `command not found` 或没有任何反应：
1.  这说明你的电脑还是“出厂设置”。
2.  请前往 Python 官网下载安装包：[python.org](https://www.python.org/downloads/)
3.  下载最新版（黄色按钮），一路点击“下一步”安装即可。
    *   **重要提示 (Windows)**：安装时务必勾选 **"Add Python to PATH"**（将 Python 添加到环境变量），否则你在终端里还是找不到它！

---

## 2. 什么是 REPL (Read-Eval-Print Loop)？

既然引擎已经就位，我们不需要像写作文一样写完整个文件才运行。Python 允许我们直接“对话”。

**REPL** 是 Python 的“即时聊天窗口”，它的原理是四个步法循环往复：
1.  **Read (读)**：读取你输入的代码。
2.  **Eval (算)**：执行逻辑。
3.  **Print (印)**：把结果打印在屏幕上。
4.  **Loop (回)**：等待下一条指令。

**工程直觉**：REPL 是为了**快速实验**。当你不知道某行代码会不会报错时，先在这里试一下，是高级工程师的日常习惯。

---

## 3. 建立连接：呼叫解释器

继续在终端里，输入对应的启动暗号：

*   **Mac**: 输入 `python3` 并回车。
*   **Win**: 输入 `python` 并回车。

当你看到 **`>>>`** 这个符号出现时，说明你已经成功“黑入”了 Python 的内部。

**⚠️ 警告**：此时你已经离开了 Shell。刚才学的 `ls`、`cd` 在这里统统失效。你现在是在和 Python 对话，不是操作系统。

---

## 4. 专业级交互实验

现在让我们在这个“沙盒”里做点实验。

### 4.1 变量与内存（Namespace）
逐行输入以下代码：
```python
>>> x = 3.14
>>> y = 2
>>> x * y
6.28
```
**底层逻辑**：这些变量现在活在内存里。只要你不关掉窗口，它们就一直存在。

### 4.2 自省 (Introspection)：工程师的显微镜
与其瞎猜，不如直接问 Python。這是 AI 時代防幻觉的最強工具：

*   **`type(x)`**：问 Python "x 是什么类型？"（它会告诉你是 float 浮点数）。
*   **`help(print)`**：查看 `print` 函数的官方说明书（按 `q` 退出说明书）。
*   **`globals()`**：看看现在的内存里到底存了哪些全局变量。

---

## 5. 机制对齐：REPL vs. 脚本 (Script)

这是新人最常有的困惑：“为什么我在 REPL 里输入 `1 + 1` 会出 `2`，但在文件夹里的 `.py` 文件写 `1 + 1` 运行后什么都不显示？”

*   **REPL 逻辑**：为了方便，它自带“自动打印”。
*   **Script 逻辑**：为了严谨，如果你不写 `print()`，程序就会默默计算完然后结束，不会告诉你任何事。

**结论**：在写文件时，永远不要忘了 `print()`。

---

## 6. 异常追踪 (Traceback)

当代码崩坏时，REPL 会吐出一段报错。别怕，这是它在告诉你错哪了。
```python
>>> 10 / 0
ZeroDivisionError: division by zero
```
这就是著名的“除以零错误”。读懂报错是工程师的第一课。

---

## 7. 紧急脱离：逃离“黑洞”

很多新手在 `>>>` 界面想回 Shell 做 `ls` 操作，结果因为没退出而在 Python 里疯狂报错。

*   **快捷键退出 (推荐)**：
    *   Mac: `Ctrl + D`
    *   Win: `Ctrl + Z` 然后按下 Enter。
*   **显式退出**：输入 `exit()` 并回车。

---

## 8. 工程自检 (Checkpoint)

### ✅ 验收标准 (Pass Criteria)
1.  **环境确认**：在终端运行 `python --version` (或 `python3`) 能看到版本号。
2.  **拉起**：成功通过终端进入 `>>>` 界面。
3.  **侦测**：
    *   定义 `a = "AI"`。
    *   运行 `type(a)` 确认它是字符串 (str)。
    *   运行 `a.upper()` 看到 'AI'。
4.  **内省**：运行 `globals()`，确认 `a` 确实在内存列表中。
5.  **脱出**：通过快捷键快速切回 Shell。

### 🛑 常见故障 (Fail Patterns)
*   **误区**：在 `>>>` 下敲 `ls` —— 只有退出到 Shell 才能控操系统。
*   **误区**：Windows 安装时没勾选 "Add to PATH"，导致输入 `python` 说找不到命令。

---

> [!IMPORTANT]
> **本课结语**
>
> 我们刚刚完成了与系统原生 Python 的“第一次握手”。
>
> 然而警告：直接给系统自带的 Python 安装库是一个极大的工程禁忌。
> 那样会把原本干净的系统搞得一团糟。我们要养成“实验室思维”——为每个项目搭建一套隔离的虚拟环境。
>
> **下一课：Conda —— 打造你的工程无尘实验室。**
